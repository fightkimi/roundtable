{
  "name": "roundtable-seminar",
  "version": "0.1.0",
  "private": true,
  "description": "Local AI roundtable seminar app with dynamic guests, attachments, freeform prompts, topic suggestions, history, and summaries.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
